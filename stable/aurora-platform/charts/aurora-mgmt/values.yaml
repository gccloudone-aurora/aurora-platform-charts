global:
  helm: {}
    # Override all Helm applications to pull
    # their helm chart from the following
    # repository.
    #
    # You can override specific charts below
    # in the components section.
    # repository: https://myrepo.example.ca

  container: {}
    # Override all applications to pull
    # their container from the following
    # registry.
    #
    # You can override specific charts below
    # in the components section.
    # registry: docker.io

  namespace:
    helm:
      chart:
      targetRevision:
      repository:

  raw:
    helm:
      chart:
      targetRevision:
      repository:

  # Cluster name for the Application targets.
  cluster: in-cluster

  # Which project to place applications in.
  project: aurora-app

  # Ingress Domain
  ingressDomain: example.ca

  # Ingress Class Name
  # ingressClassName: ingress-istio-controller

  # Azure-specific settings.
  # load_balancer_subnet_name: ""
  # subscriptionId: ""
  # tenantId: ""

data:
  mgmt:
    enabled: true

# Role Based Access Control
rbac:
  # Groups and users that should be bound to the platform-admin role.
  platformAdmin:
    groups: []
    users: []
  # Configurations for the platform-operator-daily role.
  # A read-only aggregated role which uses the
  # 'rbac.ssc-spc.gc.ca/aggregate-to-platform-operator-view: "true"'
  # label as the selector.
  platformOperator:
    groups: []
    users: []

############
### MGMT ###
############
components:

  #####################
  ### Argo Operator ###
  #####################
  argoOperator:
    helm:
      # chart: argocd-operator
      # repository: https://gccloudone-aurora.github.io/aurora-platform-charts
      targetRevision: 0.0.13
    containerSecurityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
      runAsNonRoot: true
      seccompProfile:
        type: RuntimeDefault
    imagePullSecrets: []
    image:
      # registry: quay.io
      repository: argoprojlabs/argocd-operator
      tag: "v0.16.0"
      # pullPolicy: IfNotPresent

    priorityClassName: platform-cluster-medium

    replicas: 1
    resources: {}
      # limits:
      #   cpu: ""
      #   memory: ""
      # requests:
      #   cpu: ""
      #   memory: ""
    nodeSelector:
      kubernetes.io/os: linux
      node.ssc-spc.gc.ca/purpose: system
    tolerations:
      - key: CriticalAddonsOnly
        operator: Exists
    affinity: {}

  argoSolution:
    enabled: true

    project:
      global:
        sourceRepos: []

    argocdInstance:
      helm:
      # chart: argocd-instance
      # repository: https://gccloudone-aurora.github.io/aurora-platform-charts
      # targetRevision: 0.13.0

      # disableAdmin: true
      # ingressDomain: ""

        targetRevision: 0.17.0
      nodeSelector:
        kubernetes.io/os: linux
        node.ssc-spc.gc.ca/purpose: system
      tolerations:
        - key: CriticalAddonsOnly
          operator: Exists

      argocdVaultPlugin:
        env: {}
          # ARGOCD_ENV_AVP_TYPE: ""

          # # Azure
          # ARGOCD_ENV_AZURE_TENANT_ID: ""
          # ARGOCD_ENV_AZURE_CLIENT_ID: ""
          # ARGOCD_ENV_AZURE_CLIENT_SECRET: ""

          # # AWS
          # ARGOCD_ENV_AWS_REGION: ""

      workloadIdentity:
        enabled: false
        # clientId: ""

      sourceNamespaces:
        - "/^gitops-((?!-system$).)*$/"        # starts with "gitops-" & does not end with "-system"

      oidcAuthSP:
        credentials: {}
      #     tenantID: ""
      #     clientID: ""
      #     clientSecret: ""

      netpol: {}
        # enabled: false

      notifications: {}
        # enabled: false

      server:
        service: {}
          # type: ""

      repo:
        resources: {}
          # limits:
          #   cpu: ""
          #   memory: ""
          # requests:
          #   cpu: ""
          #   memory: ""

      controller:
        resources: {}
          # limits:
          #   cpu: ""
          #   memory: ""
          # requests:
          #   cpu: ""
          #   memory: ""
      applicationSet:
        resources: {}
          # limits:
          #   cpu: ""
          #   memory: ""
          # requests:
          #   cpu: ""
          #   memory: ""
        sourceNamespaces:
          - "/^gitops-((?!-system$).)*$/"       # starts with "gitops-" & does not end with "-system"
        scmProviders: []

      redis:
        resources: {}
          # limits:
          #   cpu: ""
          #   memory: ""
          # requests:
          #   cpu: ""
          #   memory: ""

      register:
        clusters: {}
        repositories: {}
        repositoryCreds: {}

  #######################
  ### Argo Foundation ###
  #######################
  argoFoundation:
    argocdInstance:
      helm:
      # chart: argocd-instance
      # repository: https://gccloudone-aurora.github.io/aurora-platform-charts
      # targetRevision: 0.13.0

      # disableAdmin: true
      # ingressDomain: ""

        targetRevision: 0.17.0
      nodeSelector:
        kubernetes.io/os: linux
        node.ssc-spc.gc.ca/purpose: system
      tolerations:
        - key: CriticalAddonsOnly
          operator: Exists

      argocdVaultPlugin:
        env: {}
          # ARGOCD_ENV_AVP_TYPE: ""

          # # Azure
          # ARGOCD_ENV_AZURE_TENANT_ID: ""
          # ARGOCD_ENV_AZURE_CLIENT_ID: ""
          # ARGOCD_ENV_AZURE_CLIENT_SECRET: ""

          # # AWS
          # ARGOCD_ENV_AWS_REGION: ""

      workloadIdentity:
        enabled: false
        # clientId: ""

      oidcAuthSP:
        credentials: {}
      #     tenantID: ""
      #     clientID: ""
      #     clientSecret: ""

      netpol: {}
        # enabled: false

      notifications: {}
        # enabled: false

      server:
        service: {}
          # type: ""

      repo:
        resources: {}
          # limits:
          #   cpu: ""
          #   memory: ""
          # requests:
          #   cpu: ""
          #   memory: ""
        serviceAcccount: ""
        annotations: {}

      controller:
        resources: {}
          # limits:
          #   cpu: ""
          #   memory: ""
          # requests:
          #   cpu: ""
          #   memory: ""
      applicationSet:
        resources: {}
          # limits:
          #   cpu: ""
          #   memory: ""
          # requests:
          #   cpu: ""
          #   memory: ""

      redis:
        resources: {}
          # limits:
          #   cpu: ""
          #   memory: ""
          # requests:
          #   cpu: ""
          #   memory: ""

      register:
        clusters: {}
        repositories: {}
        repositoryCreds: {}

    argocdProjects:
      platform:
        # applicationSet:
        #   generator:
        #     git:
        #       repoURL
        #       revision
        #       files:
        #   template:
        #     source:
        #       repoURL

      solutions:
        # applicationSet:
        #   generator:
        #     git:
        #       repoURL:
        #       revision:
        #       files:
        #   template:
        #     source:
        #       repoURL:
        #       helmValues:
        #         argocd:
        #           project:
        #             sourceRepos: ['*']

      syncWindows:
        - clusters:
            - '*'
          duration: 5h
          kind: allow
          manualSync: true
          schedule: '* 19 * * 2,4'
          timeZone: America/Toronto

  #######################
  ### Bill of Landing ###
  #######################
  billOfLanding:
    enabled: false

    helm:
      # chart: bol
      # repository: https://gccloudone-aurora.github.io/bol
      targetRevision: 0.0.1

    image:
      # registry: docker.io
      repository: bol
      # tag: v0.0.1

    nodeSelector:
      kubernetes.io/os: linux
      node.ssc-spc.gc.ca/purpose: system
    tolerations:
      - key: CriticalAddonsOnly
        operator: Exists

    config:
      artifactRepository: {}
        # provider: azure
        # azure:
        #   storageAccountName: <storage-account-name>
        #   storageAccountContainerName: <container-name>
        #   useAzureCliCredentials: false

      clusters: []
        # - name:
        #   subscription:
        #   kubecostURL:
        #   kubernetesAuth:
        #     method: manual
        #     manual:
        #       host:
        #       bearerToken:
        #       tlsClientConfig:
        #         insecure: true
        #         caData:

      # fileNameSuffix: ""
      # maximumReportingWindowInDays: 30

  ############
  ### Loki ###
  ############
  loki:
    enabled: false

    helm:
      # chart: loki
      # repository: https://grafana.github.io/helm-charts
      targetRevision: 6.55.0

    image:
      # registry: docker.io
      repository: grafana/loki
      # tag: v3.3.0

    nodeSelector:
      kubernetes.io/os: linux
      node.ssc-spc.gc.ca/purpose: system
    tolerations:
      - key: CriticalAddonsOnly
        operator: Exists

    basicAuth: {}
      # username:
      # password:
      # htpasswd:
      # existingSecret:

    logStorageLocation: {}
      # storageAccountName: <storage-account-name>
      # storageAccountKey: <storage-account-key>

  ##############
  ### Thanos ###
  ##############
  # Thanos: Querier, Store Gateway, Compactor (Central and Shared)
  # Purpose: long-term metrics  in the mgmt cluster.
  # Takes 2 hour TDSB blocks from spokes and sends to object store
  # Allows for querable blocks as well (querier and the store gateway)
  # Compactor is data management
  # Query mode is blob-only - no cross-cluster gRPC
  thanos:
    enabled: false

    # Uses the same thanos side car image thanos/thanos image
    # ensure the image tag is >= the sidecar's Thanos version (kube-prometheus-stack 86.0.2 ships v0.41.0)
    image:
      # registry: quay.io
      repository: thanos/thanos
      tag: "v0.41.0"
      # pullPolicy: IfNotPresent

    logLevel: info

    objectStorage:
      auth: workloadIdentity
      # List of buckets for querying. Intentionally single bucket but additonal buckets can be added
      # if a single shared bucket is not possible.
      # Each item has it's own objstore secret + store Gateway + compactor
      #   name - short DNS-safe key used in resource names (thanos-*-<name>)
      #   clientId - workload-identity UAMI federated to this bucket's store/compactor SAs
      buckets:
        - name: default
          storageAccountName: ""
          containerName: thanos
          endpoint: blob.core.windows.net
          clientId: ""

    priorityClassName: platform-cluster-medium

    ### Querier — stateless PromQL fan-out over the Store Gateway(s); the Grafana datasource ###
    query:
      replicas: 2
      # Dedupe HA/replica series; kube-prometheus-stack stamps prometheus_replica per pod.
      replicaLabels:
        - prometheus_replica
      resources: {}
      nodeSelector:
        kubernetes.io/os: linux
        node.ssc-spc.gc.ca/purpose: system
      tolerations:
        - key: CriticalAddonsOnly
          operator: Exists
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 1
            podAffinityTerm:
              topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels:
                  app.kubernetes.io/name: thanos-query

    ### Store Gateway — serves historical blocks from Blob (one deployed per bucket) ###
    storeGateway:
      # Replicas per bucket. Store Gateway is read-only, so >1 is safe (HA)
      replicas: 1
      persistence:
        size: 20Gi
        storageClassName: default
      resources: {}
      nodeSelector:
        kubernetes.io/os: linux
        node.ssc-spc.gc.ca/purpose: system
      tolerations:
        - key: CriticalAddonsOnly
          operator: Exists
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 1
            podAffinityTerm:
              topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels:
                  app.kubernetes.io/name: thanos-storegateway

    ### Compactor — compaction, downsampling, retention. Singleton per bucket (replicas fixed at 1) ###
    compactor:
      # retention per resolution (Prometheus/Thanos durations, e.g. 30d/90d/1y).
      # until compactor runs, blob grows unbounded
      retention:
        raw: 30d
        fiveMin: 90d
        oneHour: 1y
      persistence:
        size: 50Gi
        storageClassName: default
      resources: {}
      nodeSelector:
        kubernetes.io/os: linux
        node.ssc-spc.gc.ca/purpose: system
      tolerations:
        - key: CriticalAddonsOnly
          operator: Exists
      affinity: {}

    # Thanos Grafana Datasource
    grafana:
      datasource:
        enabled: true
      dashboard:
        enabled: true
      instanceSelector:
        dashboards: aurora
      namespace: grafana-system

    # Scrape Querier/Store Gateway/Compactor via ServiceMonitors
    serviceMonitor:
      enabled: true
