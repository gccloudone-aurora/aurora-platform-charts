{{ if .Values.components.kubebench.enabled -}}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "{{ .Values.global.cluster }}-kube-bench-netpol"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.global.project }}
  source:
    chart: {{ .Values.global.raw.helm.chart }}
    repoURL: {{ default .Values.global.helm.repository .Values.global.raw.helm.repository }}
    targetRevision: {{ .Values.global.raw.helm.targetRevision }}
    helm:
      releaseName: kube-bench-netpol
      values: |
        resources:
          - apiVersion: networking.k8s.io/v1
            kind: NetworkPolicy
            metadata:
              name: allow-egress-to-azure-storage-from-kube-bench
              namespace: kube-bench-system
            spec:
              podSelector:
                matchLabels:
                  app.kubernetes.io/name: kube-bench
              egress:
                - ports:
                    - protocol: TCP
                      port: 443    
                  to:
                    - ipBlock:
                        cidr: 0.0.0.0/0
              policyTypes:
                - Egress
          - apiVersion: networking.k8s.io/v1
            kind: NetworkPolicy
            metadata:
              name: allow-ingress-from-platform-logging-to-kube-bench
              namespace: kube-bench-system
            spec:
              ingress:
              - ports:
                - port: 8080
                  protocol: TCP
                from:
                - namespaceSelector:
                    matchLabels:
                      kubernetes.io/metadata.name: prometheus-system
                  podSelector:
                    matchLabels:
                      app.kubernetes.io/name: grafana
                - namespaceSelector:
                    matchLabels:
                      kubernetes.io/metadata.name: fluent-operator-system
                  podSelector:
                    matchLabels:
                      app.kubernetes.io/name: fluentd
                - namespaceSelector:
                    matchLabels:
                      kubernetes.io/metadata.name: event-logging-system
                  podSelector:
                    matchLabels:
                      app.kubernetes.io/name: kubernetes-event-exporter
              podSelector:
                matchLabels:
                  app.kubernetes.io/component: gateway
              policyTypes:
              - Ingress
  destination:
    name: {{ .Values.global.cluster }}
    namespace: kube-bench-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

{{- end }}