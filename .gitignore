packages
index.yaml
rendered.yaml
.vscode
*.log
.env

# Helm CI render output (see .github/workflows/render-validate-helm.yaml)
rendered/

# Umbrella chart lock file is regenerated by `helm dependency build`.
# Subcharts for aurora-platform are vendored as directories, not pinned here.
stable/aurora-platform/Chart.lock

# Local scratch
tmp/
